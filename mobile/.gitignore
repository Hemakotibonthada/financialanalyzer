node_modules/
.expo/
dist/
web-build/
*.log

# Native folders are generated by `expo prebuild` and must not be committed:
# committing them turns the managed workflow into bare RN by accident.
/android
/ios

# Local secrets
.env
.env.local
