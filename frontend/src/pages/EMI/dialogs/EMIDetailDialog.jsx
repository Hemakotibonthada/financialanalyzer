// Placeholder
import React from 'react';
import { Dialog } from '@mui/material';

const EMIDetailDialog = ({ open, onClose, selectedEMI, selectedEmiChartData, onEdit, onDelete, onMarkPaid }) => {
  return <Dialog open={open} onClose={onClose}></Dialog>;
};

export default EMIDetailDialog;
